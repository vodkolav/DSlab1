[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Rockets"
version = "0.1.0"
description = "SRM grain reverse design"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []